# Chapter 1 
-----------------------------
## Data Definitions & Types of Sources 
**Data** is basic values and facts
- Analog Data is continuous and analogous
- Digital data are discrete and broken up into a limited number of elements 
## Types of Data 
- In early days data consisted primarly of text and numbers but in modern day there are lots of different multimedia data ypes like audio, images, graphics, and video
- ALL data types are stored as binary digits
## Databases 
a **database** is an organized & structured collection of data.
- Each row is called a record and each column is called a field 
- a **database management system** is a specialized software to inpuyt, store, retreive and manage all the data. 
- Data are basic values while and often unstructured and lack context
- information is organized and structured data that helps us answer questions
------------------------------
## Database examples, schema & types 
- strictly defined databased are computer structures that save, organize, protect and deliver data 
- The simplest form of database is a text database 
- Desktop database programs are another type of database that is more complex than a text database but intended for a single user. examples would be excel, or microsoft access 
- Relational databases are the most common database systems. They include SQL servers, Oracle databases, Sybase, Informix, and mySQL. RDBMS (Relational Database Management System) features much better performance for managing data over desktop database programs 
- RDBMS allow THOUSANDS of users to work with the data at the same time, created advanced security for access to the data. 
- RDBMS store data in columns and rows, which make up a table. A table is like a spreadsheet. 
- A set of tables makes up a schema, and and number of schema create a database. Many databases can be created on a single server 
# 1.7
## Models of Database Management Systems 
**Flat file** is the most basic way to organize data. A single table with a large number of records and fields. 
**Hierarchical DBMS** is a parent-child relationship. Each data item is subordinate to another one. This is a tree-like structure. 
**Network DBMS** every data item can be related to many others. This is more like a graph. 
**Relational DBMS** all data is rganized in the form of tables where each row is a record and each column is a field. A relational DBMS uses multiple tables to organize the data. 
**Object-Oriented DBMS** handle many newer data types like audio * video. Everythig is stored as an object in the database. 
# 1.8 
## Advantages of DBMS 
